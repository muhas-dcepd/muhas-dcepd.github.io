# DCEPD REDCap Continuity / Handoff

## What the two REDCap projects do

**Project 75** is the authoritative short-course registry. One master record represents a course; deliveries are separate repeating `course_run_log` instances.  
**Project 79** is the short-course application registry. `applied_course_id` is mapped to Project 75 through the maintained crosswalk.

## What staff enter manually

Staff maintain source facts such as course name, submission/review information, latest accreditation date, approval date/reference, organising unit, and individual run details. Approval references are never generated by R.

Metadata changes—choice lists, year lists, school/department lists and Project 79 dropdown additions—are **manual REDCap administration**. Never use the API to write the data dictionary.

## What R calculates

R derives lifecycle status, dormancy, accreditation validity, curriculum type, catalogue visibility, run summaries, calendar/fiscal fields and Project 79 applicant counts.

## Locked identity rules

- Existing course codes are historical identifiers and are never silently rewritten.
- New code format: `DCEPD/<unit>/<record_id padded to 3>/<first accreditation year>`.
- Code generation requires a blank code, formal approval, current accreditation date, first-ever accreditation date and valid organisational code.
- Reaccreditation updates the latest accreditation date but never changes the first-ever date or course code.
- Historical `/1900` identifiers remain known pending-accreditation exceptions until genuine accreditation is completed.

## Accreditation and catalogue rules

- Accreditation validity is three years from the latest accreditation date.
- `Current`: more than six months remain.
- `Due soon`: due now or within six months.
- `Overdue`: anniversary has passed.
- Dormant: last valid run is more than two years before the reference date.
- Public catalogue = Current/Due soon AND not dormant. Never-run newly accredited courses may be public.

## Applicant counts

Project 79 is read-only to the automation. Existing mapped application records are counted. Successful mapping with no applications gives 0. Failed export or unresolved used mapping never becomes a false zero. The timestamp changes only when the stored count changes.

## Routine operation

1. GitHub scheduled/manual refresh starts.
2. Structural guards confirm 48 Project 75 fields/two instruments and 53 Project 79 fields/one instrument.
3. Data and metadata are exported.
4. QC and proposed record state are calculated.
5. QC errors block LIVE writes.
6. Project 75 record updates are imported in small batches and each batch is verified.
7. Project 79 is re-exported read-only; applicant counts are refreshed.
8. Project 75 is re-exported and final expected state verified.
9. A verified archive is created only after success.
10. GitHub then refreshes the public catalogue/dashboard.

## Do not change

- Do not enable metadata API import.
- Do not renumber/delete existing Project 79 dropdown choice codes.
- Do not recode historical course identifiers merely to match a current school name.
- Do not treat missing data as zero unless the source export/mapping establishes a true zero.
- Do not bypass batch verification to speed up imports.

## If something fails

Stop the run; do not manually force later stages. A new run always begins with a fresh export and will propose only what remains incorrect. For structural loss, restore the correct dictionary first and check whether stored data reappear before importing any record backup.
